import BlogConfig from '@/config';
export const POSTDIR_NOT_FOUND = `${BlogConfig.postDir}가 프로젝트 루트에 존재하지 않습니다. 먼저 경로를 생성하세요.`;
export const CONFIG_ALREADY_EXIST = 'blog.config.ts가 이미 존재합니다. 기본 설정을 사용하려면 파일을 삭제해주세요.';